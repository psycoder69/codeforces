#include <bits/stdc++.h>
using namespace std;

const int MAX = 1e5 + 5;
const int MOD = 1e9 + 7;

int main()
{
    ios_base :: sync_with_stdio (false);
    cin.tie (NULL);
    cout.tie (NULL);

    int t, n, m;
    cin >> t;

    while (t --)
    {
        cin >> n >> m;

        vector <long long> nums(n);

        for (long long& num : nums) cin >> num;

        char c;

        int l, r;

        while (m --)
        {
            cin >> c >> l >> r;
        }
    }

    return 0;
}